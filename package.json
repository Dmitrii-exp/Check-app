{
  "name": "check-app",
  "version": "1.0.0",
  "private": true,
  "description": "Check App — система контроля технического обслуживания",
  "scripts": {
    "dev": "npx serve .",
    "preview": "npx serve .",
    "cap:sync": "npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.9.2"
  }
}
